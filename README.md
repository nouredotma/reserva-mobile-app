# Reserva Mobile

Flutter mobile application for Reserva (Android and iOS).

## Scope

- Mobile client for the Reserva product
- Uses in-project Dart mock data (`lib/core/data/mock/mock_data.dart`)
- Targets Android and iOS

## Run

```bash
flutter pub get
flutter run
```
